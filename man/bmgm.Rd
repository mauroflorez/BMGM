% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bmgm.R
\name{bmgm}
\alias{bmgm}
\title{Bayesian Mixed Graphical Model}
\usage{
bmgm(
  X,
  type,
  nburn = 1000,
  nsample = 1000,
  theta_priors,
  v_0 = 0.05,
  v_1 = 1,
  pi_beta,
  seed,
  context_spec = T,
  bfdr = 0.05,
  cont = FALSE,
  ...
)
}
\arguments{
\item{X}{A numeric matrix or data frame containing the data (mixed type variables).}

\item{type}{A character vector indicating the type of each variable.
Options: "c" (continuous), "d" (discrete), "z" (zero-inflated count), "m" (categorical).}

\item{nburn}{Number of burn-in iterations for MCMC. Default is 1000.}

\item{nsample}{Number of MCMC samples to collect after burn-in. Default is 1000.}

\item{theta_priors}{List of priors for node parameters.}

\item{v_0}{Prior variance for the spike in spike-and-slab. Default is 0.05.}

\item{v_1}{Prior variance for the slab in spike-and-slab. Default is 1.}

\item{pi_beta}{Prior probability of edge inclusion. Default is `2/(p-1)`.}

\item{seed}{Random seed for reproducibility.}

\item{context_spec}{Logical, whether to compute the context-specific adjacency matrix. Default is TRUE.}

\item{bfdr}{Bayesian False Discovery Rate threshold for edge selection. Default is 0.05.}

\item{cont}{Logical, whether to transform continuous variables. Default is FALSE.}

\item{...}{Additional arguments.}
}
\value{
A list containing:
\describe{
  \item{post_Beta}{Posterior samples of the precision matrix.}
  \item{post_theta}{Posterior samples of node parameters.}
  \item{post_Z}{Posterior samples of edge indicators.}
  \item{adj_Beta}{Estimated adjacency matrix for dependencies.}
  \item{adj_G}{Adjacency matrix for edge selection.}
  \item{lambda}{Estimated transformation parameter.}
}
}
\description{
Implements a Bayesian approach for inference on mixed graphical models.
Estimates conditional independencies between variables of mixed types
(continuous, discrete, categorical, zero-inflated count data) using an MCMC
algorithm for posterior inference.
}
\examples{
# Simulated example data
set.seed(123)
n <- 100
X1 <- rpois(n, lambda = 3)
X2 <- rnorm(n, mean = 5)
X3 <- rnorm(n, mean = X2)
X4 <- rnorm(n, mean = X1)
X <- cbind(X1,X2,X3,X4)
type <- c("d", "c", "c", "c")

# Fit Bayesian Mixed Graphical Model
fit <- bmgm(X, type, nburn = 1000, nsample = 2000)

# Print adjacency matrix
print(fit$adj_G)

}
